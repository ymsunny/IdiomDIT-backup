# Copy this file to .env and fill in your key.
# config.py loads .env automatically at import time.

OPENAI_API_KEY=your-api-key-here

# Optional: only needed if you're not using the official OpenAI API
# (e.g. an OpenAI-compatible relay or self-hosted endpoint).
# OPENAI_API_BASE_URL=https://api.openai.com/v1
