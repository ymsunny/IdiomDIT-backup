# Copy this file to .env and fill in your key.
# config.py loads .env automatically at import time.

OPENAI_API_KEY=your-api-key-here

# Note: the LLM-judge API endpoint (API_BASE_URL) is NOT read from .env --
# it's a hardcoded constant in config.py. Edit it there directly if you're
# not using the same OpenAI-compatible relay the original experiments used.
